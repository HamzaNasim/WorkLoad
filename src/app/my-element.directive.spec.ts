import { MyElementDirective } from './my-element.directive';

describe('MyElementDirective', () => {
  it('should create an instance', () => {
    const directive = new MyElementDirective();
    expect(directive).toBeTruthy();
  });
});
